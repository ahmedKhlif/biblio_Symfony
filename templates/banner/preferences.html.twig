{% extends 'base.html.twig' %}

{% block title %}Mes Préférences de Bannière - Biblio{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <!-- Page Heading -->
            <div class="mb-4">
                <h1 class="h2">
                    <i class="fas fa-bell"></i> Préférences de Bannière
                </h1>
                <p class="text-muted">Gérez les bannières et annonces que vous souhaitez voir</p>
            </div>

            <!-- Information Card -->
            <div class="alert alert-info mb-4">
                <i class="fas fa-info-circle mr-2"></i>
                <strong>À propos de vos préférences:</strong>
                Les bannières que vous masquez seront mémorisées et n'apparaîtront plus jusqu'à ce que vous les réinitialisiez.
            </div>

            {% if visibleBanners|length > 0 %}
                <!-- Visible Banners Section -->
                <div class="card shadow mb-4">
                    <div class="card-header py-3">
                        <h5 class="m-0">Bannières Visibles</h5>
                    </div>
                    <div class="card-body">
                        <p class="text-muted small">Ces bannières sont actuellement visibles pour vous</p>

                        {% for banner in visibleBanners %}
                        <div class="d-flex justify-content-between align-items-center mb-3 pb-3 border-bottom">
                            <div>
                                <h6 class="mb-1">{{ banner.title }}</h6>
                                <small class="text-muted">
                                    <span class="badge badge-{{ banner.type == 'promotion' ? 'success' : (banner.type == 'warning' ? 'warning' : (banner.type == 'announcement' ? 'info' : 'primary')) }}">
                                        {{ banner.typeLabel }}
                                    </span>
                                </small>
                            </div>
                            <form method="POST" action="{{ path('app_banner_hide') }}" class="d-inline">
                                <input type="hidden" name="banner_id" value="{{ banner.id }}">
                                <input type="hidden" name="_token" value="{{ csrf_token('hide_banner') }}">
                                <button type="submit" class="btn btn-sm btn-outline-danger">
                                    <i class="fas fa-eye-slash"></i> Masquer
                                </button>
                            </form>
                        </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}

            {% if hiddenBanners|length > 0 %}
                <!-- Hidden Banners Section -->
                <div class="card shadow mb-4 border-warning">
                    <div class="card-header py-3 bg-warning">
                        <h5 class="m-0 text-white">
                            <i class="fas fa-eye-slash mr-2"></i>Bannières Masquées ({{ hiddenBanners|length }})
                        </h5>
                    </div>
                    <div class="card-body">
                        <p class="text-muted small">Ces bannières sont masquées et ne s'affichent pas pour vous</p>

                        {% for preference in hiddenBanners %}
                        <div class="d-flex justify-content-between align-items-center mb-3 pb-3 border-bottom">
                            <div>
                                <h6 class="mb-1">{{ preference.banner.title }}</h6>
                                <small class="text-muted">
                                    Masqué le {{ preference.hiddenAt|date('d/m/Y à H:i') }}
                                </small>
                                <br>
                                <small class="text-muted">
                                    <span class="badge badge-{{ preference.banner.type == 'promotion' ? 'success' : (preference.banner.type == 'warning' ? 'warning' : (preference.banner.type == 'announcement' ? 'info' : 'primary')) }}">
                                        {{ preference.banner.typeLabel }}
                                    </span>
                                </small>
                            </div>
                            <form method="POST" action="{{ path('app_banner_show') }}" class="d-inline">
                                <input type="hidden" name="banner_id" value="{{ preference.banner.id }}">
                                <input type="hidden" name="_token" value="{{ csrf_token('show_banner') }}">
                                <button type="submit" class="btn btn-sm btn-outline-success">
                                    <i class="fas fa-eye"></i> Afficher
                                </button>
                            </form>
                        </div>
                        {% endfor %}

                        <div class="mt-3">
                            <form method="POST" action="{{ path('app_banner_reset_all') }}" style="display:inline;">
                                <input type="hidden" name="_token" value="{{ csrf_token('reset_all_banners') }}">
                                <button type="submit" class="btn btn-warning btn-sm" onclick="return confirm('Êtes-vous sûr de vouloir réinitialiser tous les masquages ?')">
                                    <i class="fas fa-redo"></i> Réinitialiser Tous
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            {% endif %}

            {% if visibleBanners|length == 0 and hiddenBanners|length == 0 %}
                <div class="alert alert-info">
                    <i class="fas fa-info-circle mr-2"></i>
                    Vous n'avez pas encore de préférences de bannière. Les bannières s'affichent par défaut jusqu'à ce que vous les masquiez.
                </div>
            {% endif %}

            <!-- Back Button -->
            <div class="mt-4">
                <a href="{{ path('app_dashboard') }}" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Retour au Tableau de Bord
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    .border-bottom {
        border-bottom: 1px solid #e3e6f0 !important;
    }
    
    .badge {
        font-size: 0.85rem;
    }
</style>
{% endblock %}
