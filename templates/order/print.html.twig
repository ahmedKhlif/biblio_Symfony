<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Commande {{ order.orderNumber }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Arial', sans-serif;
            color: #333;
            background: #fff;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
            border-bottom: 3px solid #009688;
            padding-bottom: 20px;
        }
        .logo {
            font-size: 28px;
            font-weight: bold;
            color: #009688;
        }
        .order-info {
            text-align: right;
        }
        .order-info h2 {
            margin-bottom: 10px;
            color: #009688;
        }
        .order-number {
            font-weight: bold;
            margin-bottom: 5px;
        }
        .order-date {
            color: #666;
            font-size: 14px;
        }
        .addresses {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }
        .address-block {
            padding: 20px;
            background: #f5f5f5;
            border-radius: 5px;
        }
        .address-title {
            font-weight: bold;
            margin-bottom: 10px;
            color: #009688;
        }
        .address-content {
            font-size: 14px;
            line-height: 1.6;
            color: #666;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }
        thead {
            background: #009688;
            color: white;
        }
        th {
            padding: 15px;
            text-align: left;
            font-weight: bold;
        }
        td {
            padding: 15px;
            border-bottom: 1px solid #ddd;
        }
        tbody tr:nth-child(even) {
            background: #f9f9f9;
        }
        .item-description {
            font-weight: 600;
        }
        .text-right {
            text-align: right;
        }
        .summary {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 40px;
        }
        .summary-box {
            width: 300px;
            border: 2px solid #009688;
            border-radius: 5px;
            padding: 20px;
        }
        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 16px;
        }
        .summary-row.total {
            font-weight: bold;
            font-size: 18px;
            color: #009688;
            border-top: 2px solid #009688;
            padding-top: 10px;
            margin-top: 10px;
        }
        .order-status {
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            font-weight: bold;
        }
        .status-paid {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .status-pending {
            background: #fff3cd;
            color: #856404;
            border: 1px solid #ffeaa7;
        }
        .status-processing {
            background: #cce5ff;
            color: #004085;
            border: 1px solid #b8daff;
        }
        .status-shipped {
            background: #d1ecf1;
            color: #0c5460;
            border: 1px solid #bee5eb;
        }
        .status-delivered {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
        .status-cancelled {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        .footer {
            border-top: 1px solid #ddd;
            padding-top: 20px;
            margin-top: 40px;
            text-align: center;
            color: #999;
            font-size: 12px;
        }
        .payment-method {
            padding: 15px;
            background: #f9f9f9;
            border-radius: 5px;
            margin-bottom: 20px;
            font-size: 14px;
        }
        .payment-method label {
            font-weight: bold;
            color: #333;
        }
        .payment-method-value {
            color: #009688;
            margin-left: 10px;
        }
        @media print {
            body {
                padding: 0;
            }
            .container {
                padding: 0;
            }
            .print-button {
                display: none;
            }
        }
        .print-button {
            margin-bottom: 20px;
            text-align: center;
        }
        .print-button button {
            background: #009688;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
        }
        .print-button button:hover {
            background: #00796b;
        }
    </style>
</head>
<body>
    <div class="print-button">
        <button onclick="window.print()">Imprimer cette commande</button>
    </div>

    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="logo">📚 BiblioApp</div>
            <div class="order-info">
                <h2>COMMANDE</h2>
                <div class="order-number">N° {{ order.orderNumber }}</div>
                <div class="order-date">Date: {{ order.createdAt|date('d/m/Y') }}</div>
            </div>
        </div>

        <!-- Order Status -->
        {% set statusClass = 'status-pending' %}
        {% set statusClass = 'status-pending' %}
        {% set statusText = 'Statut inconnu' %}
        
        {% if order.status == 'paid' %}
            {% set statusClass = 'status-paid' %}
            {% set statusText = 'Commande Payée' %}
        {% elseif order.status == 'pending' %}
            {% set statusClass = 'status-pending' %}
            {% set statusText = 'En Attente de Paiement' %}
        {% elseif order.status == 'processing' %}
            {% set statusClass = 'status-processing' %}
            {% set statusText = 'En Cours de Traitement' %}
        {% elseif order.status == 'shipped' %}
            {% set statusClass = 'status-shipped' %}
            {% set statusText = 'Expédiée' %}
        {% elseif order.status == 'delivered' %}
            {% set statusClass = 'status-delivered' %}
            {% set statusText = 'Livrée' %}
        {% elseif order.status == 'cancelled' %}
            {% set statusClass = 'status-cancelled' %}
            {% set statusText = 'Annulée' %}
        {% endif %}
        
        <div class="order-status {{ statusClass }}">
            <strong>Statut de la commande:</strong> {{ statusText }}
            {% if order.status == 'paid' and order.paidAt %}
                - Payée le {{ order.paidAt|date('d/m/Y') }}
            {% endif %}
        </div>

        <!-- Payment Method -->
        {% if order.paymentMethod %}
            <div class="payment-method">
                <label>Méthode de paiement:</label>
                <span class="payment-method-value">
                    {% if order.paymentMethod == 'stripe' %}
                        Carte bancaire (Stripe)
                    {% elseif order.paymentMethod == 'manual' %}
                        Virement bancaire
                    {% elseif order.paymentMethod == 'paypal' %}
                        PayPal
                    {% else %}
                        {{ order.paymentMethod|capitalize }}
                    {% endif %}
                </span>
            </div>
        {% endif %}

        <!-- Addresses -->
        <div class="addresses">
            <div class="address-block">
                <div class="address-title">Facturation</div>
                <div class="address-content">
                    {% if order.user.billingAddress %}
                        {{ order.user.billingAddress.prenom }} {{ order.user.billingAddress.nom }}<br>
                        {{ order.user.billingAddress.adresse }}<br>
                        {{ order.user.billingAddress.codePostal }} {{ order.user.billingAddress.ville }}<br>
                        {{ order.user.billingAddress.pays }}
                    {% else %}
                        {{ order.user.email }}<br>
                        {{ order.user.firstName|default('') }} {{ order.user.lastName|default('') }}<br>
                        Non spécifiée
                    {% endif %}
                </div>
            </div>

            <div class="address-block">
                <div class="address-title">Livraison</div>
                <div class="address-content">
                    {% if order.user.shippingAddress %}
                        {{ order.user.shippingAddress.prenom }} {{ order.user.shippingAddress.nom }}<br>
                        {{ order.user.shippingAddress.adresse }}<br>
                        {{ order.user.shippingAddress.codePostal }} {{ order.user.shippingAddress.ville }}<br>
                        {{ order.user.shippingAddress.pays }}
                    {% else %}
                        Identique à la facturation
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Items Table -->
        <table>
            <thead>
                <tr>
                    <th>Livre</th>
                    <th>Auteur</th>
                    <th class="text-right">Quantité</th>
                    <th class="text-right">Prix Unitaire</th>
                    <th class="text-right">Total</th>
                </tr>
            </thead>
            <tbody>
                {% for item in order.orderItems %}
                <tr>
                    <td class="item-description">{{ item.livre.titre }}</td>
                    <td>{{ item.livre.auteur ? item.livre.auteur.prenom ~ ' ' ~ item.livre.auteur.nom : 'N/A' }}</td>
                    <td class="text-right">{{ item.quantity }}</td>
                    <td class="text-right">{{ item.unitPriceFormatted }}</td>
                    <td class="text-right"><strong>{{ item.subtotalFormatted }}</strong></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

        <!-- Summary -->
        <div class="summary">
            <div class="summary-box">
                <div class="summary-row">
                    <span>Sous-total:</span>
                    <span>{{ order.totalAmountFormatted }}</span>
                </div>
                <div class="summary-row">
                    <span>Frais de port:</span>
                    <span>Gratuit</span>
                </div>
                <div class="summary-row total">
                    <span>Total TTC:</span>
                    <span>{{ order.totalAmountFormatted }}</span>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Merci pour votre commande chez BiblioApp!</p>
            <p>Pour toute question, contactez-nous à: support@biblioapp.com</p>
            <p style="margin-top: 10px; color: #ccc;">Facture générée le {{ "now"|date('d/m/Y H:i') }}</p>
        </div>
    </div>
</body>
</html>
